{
  "name": "gloss-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/cli && bun run build",
    "e2e": "bun e2e/run.ts && bun e2e/loop.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
