# The fact tree becomes a drawer, and closes itself

A hamburger opens the tree as a left drawer over a scrim; tapping a
fact navigates and dismisses it in one motion. The filter bar at the
top — with its search icon — is the drawer's search; the seen-progress
readout sits beside it, and the snapshot picker stays in the nav bar.
Tree rows grow to 40px touch targets.
