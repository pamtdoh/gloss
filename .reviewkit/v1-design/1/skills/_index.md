# Skills are the primary interface; the tool ships two

Claude Code (or codex) drives the whole flow through skill instructions:
`reviewkit-review` (generate, session, answer live, iterate, approve) and
`reviewkit-implement`. `skill install` copies identical content into
`.claude/skills/` or `.agents/skills/`; the skills describe conventions,
they do not police them.
