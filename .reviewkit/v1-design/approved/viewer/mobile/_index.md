# Mobile is a first-class review surface, not a scaled-down one

The same session, files, and features serve every screen; below 860px
the layout reorganizes rather than degrades. Nothing forks: one bundle,
one token layer, and the phone reaches the session over the owner's
private tailnet (`--serve-host`) while the server stays loopback-only.
Each child fact records one mobile design decision.
