# The fact tree becomes a drawer, and closes itself

A hamburger opens the tree as a left drawer over a scrim; tapping a
fact navigates and dismisses it in one motion. The drawer also hosts
what the compact header can't: the snapshot switcher and a Search
button (the palette needs no keyboard). Tree rows grow to 40px touch
targets.
