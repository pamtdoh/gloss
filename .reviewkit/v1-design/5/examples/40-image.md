# Images live inside the snapshot and travel with copies

![The file flow: facts to sidecars to approved](./assets/flow.svg)

The reference is relative, the file sits in `examples/assets/`, and the
session serves it only to the authenticated reviewer — remote image URLs
never embed.
