{
  "name": "reviewkit",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "reviewkit": "./dist/reviewkit.js"
  },
  "scripts": {
    "build": "bun build src/viewer/client.ts --target=browser --outfile src/viewer/client.gen.js && bun build src/reviewkit.ts --target=node --banner '#!/usr/bin/env node' --outfile dist/reviewkit.js && chmod +x dist/reviewkit.js",
    "typecheck": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
