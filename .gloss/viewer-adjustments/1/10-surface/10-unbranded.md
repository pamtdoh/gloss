# The page never says "reviewkit"

The browser tab and the header carry the review's own name
(`design-review`), set from the loaded data; the static page title is
a plain "Review" until then. The auth interstitial and the
unauthorized message are equally unbranded. The tool's name lives in
the terminal, where the tool is.
