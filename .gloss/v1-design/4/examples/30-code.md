# Fenced code appears when the exact shape is the claim

```json
{
  "items": [
    { "id": "a1", "type": "annotation",
      "anchor": { "quote": "last write wins" },
      "text": "Use an atomic rename." }
  ]
}
```

That is a complete sidecar — the entire review state for one fact.
