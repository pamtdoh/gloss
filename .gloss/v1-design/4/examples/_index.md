# Example facts showing every rich format the viewer renders

This group exists so the rendering itself can be reviewed: a GFM table,
a Mermaid diagram, a fenced code block, and an in-snapshot image — one
per fact, numbered in reading order. If these read well, the formats are
ready for real reviews; if not, annotate the one that doesn't.
