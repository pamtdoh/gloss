# The fact tree becomes a drawer, and closes itself

A hamburger opens the tree as a left drawer over a scrim; tapping a
fact navigates and dismisses it in one motion. The drawer hosts Search
(the palette needs no keyboard) and the seen-progress readout; the
snapshot switcher stays in the nav bar so the current snapshot is
always visible. Tree rows grow to 40px touch targets.
