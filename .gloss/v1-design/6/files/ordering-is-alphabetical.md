# Order is alphabetical; numbering is an opt-in convention

Facts sort by filename everywhere — the viewer and a terminal `ls` agree,
and no manifest or index controls order. When a group reads as a story,
numeric prefixes (`10-auth.md`, `20-sessions.md`) make narrative order the
sort order; the tool attaches no meaning to names, so this stays purely an
authoring convention.
