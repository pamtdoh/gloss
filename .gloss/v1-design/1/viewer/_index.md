# The viewer is deliberately thin and humans-only

A single vanilla-JS page served from the session: three columns, j/k
navigation, four decision keys, selection annotations, questions,
comments, undo, Finish, and Approve. No creation forms, no agent progress,
no integration hooks, and agents never call its HTTP API.
