# Structure carries the semantics; nothing validates it

An absent sidecar is agreement, deleting an item resolves it, and the
`approved/` directory existing is the approval. No schema validation,
hashing, or integrity checks exist anywhere — the conventions are followed
by instruction (the skills), not enforced by code.
