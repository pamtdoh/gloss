# Tables carry enumerations the prose would bury

| surface | writes | read by |
|---------|:------:|---------|
| `facts/*.md` | agent | human + agent |
| `*.review.json` | viewer + agent | agent |
| `approved/` | one `cp -r` | implement skill |

Wide tables scroll inside their own container instead of dragging the
reading pane sideways; headers stay left-aligned.
